import Layout from "./Layout";

const ProductNotFound = () => {
	return (
		<Layout className="mx-10">
			<span className="text-lg lg:text-4xl">Sorry, no product found!</span>{" "}
		</Layout>
	);
};

export default ProductNotFound;
